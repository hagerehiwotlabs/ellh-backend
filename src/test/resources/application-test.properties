# Test profile — values overridden by AbstractIntegrationTest @DynamicPropertySource
spring.profiles.active=test
ai.service.active-provider=mock
jwt.secret=test_jwt_secret_minimum_32_chars_long_for_hmac
